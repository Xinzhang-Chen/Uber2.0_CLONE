import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const RideOptionScreen = () => {
  return (
    <View>
      <Text>RideOptionScreen</Text>
    </View>
  );
};

export default RideOptionScreen;

const styles = StyleSheet.create({});
